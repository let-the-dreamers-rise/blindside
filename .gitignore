node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
playwright-report/
test-results/
# Raw takes from the video run. docs/demo.mp4 is the one that gets committed.
app/video-out/
docs/demo.webm
.vite/
.turbo/

# A local chain's private state store: rebuilt on every run, never shared.
midnight-level-db/
app/public/zk/

# Wallet and player secrets must never be committed.
*.seed
*.keycard
secrets/
