{
  "name": "@blindside/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "artifact": "pnpm build && node scripts/artifact-page.mjs",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blindside/contract": "workspace:*",
    "@blindside/core": "workspace:*",
    "qrcode": "1.5.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.50.1",
    "@types/qrcode": "1.5.5",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "@vitejs/plugin-react": "4.3.4",
    "typescript": "6.0.2",
    "vite": "6.0.7",
    "vite-plugin-wasm": "3.4.1"
  }
}
